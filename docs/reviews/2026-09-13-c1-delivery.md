# C1 连续迭代与待验收台账

按 v4 和 C1 实施计划推进，形成完整旅程后统一交付。自动检查不替代真机验收。

## 保留的待验收项

- **0.4.14（22）**：进入帖子时残留的静态刷新标记已修复；IPA 已交付；用户安装 0.4.15 后确认该静态刷新标记问题通过。
- **C1**：猫主页共同故事、选猫发布与回流、本人更正关联已合成 0.4.15（23）候选，统一等待设备验收。
- 之前用户已通过的地图、单行玻璃导航和报告等不重新标为未通过；相关回归继续保留。

## 实施记录

### Task 1 · 共同故事

- 新增独立公开故事投影和自包含游标；同一只猫可汇集不同作者的原帖。删除分页锚点不影响后页，头像按页批量获取。
- 猫主页以共同故事及分享入口为主；目击、照护、足迹、关注、纠错继续可达。返回页面重新读取，换账户立即清空旧内容。
- 仅公开且可见的帖子附件进入故事，缺图保留正文与重试。不会用猫档案照片冒充帖子照片。
- 定向检查：5 个 Jest 套件、32 项测试通过；TypeScript 通过；045 pgTAP 25 项通过。先记录失败再实现，审查发现的评论可见性与游标严格校验问题已补回归并修复。
- 独立审查：`c1_story_sql_review`（Terra，只读）最终无实现阻塞项。该节点尚未进行全库检查、后端部署与原生构建；后续结果见下方集中交付，设备测试仍待验收。

### 执行裁定

- 同一工作树由主代理写入，独立代理只读审查；C1 三部分共享发布/权限状态，不并行修改。
- 不为 Task 1 单独交付小版本；继续完成选猫发布和关联更正。

## 集中设备验收（尚未开始）

- 两位用户为同猫分享不同故事，猫主页保留各自作者及原帖。
- 已知猫发布、未知/多猫发布、六图编辑和旧草稿恢复。
- 本人改选/取消关联、返回新旧猫主页、删除/隐藏后的可见性。
- 失联重试保持原请求，不重复创建；换账户、断网、中文/英文、浅深色及大字。
- 0.4.14 的静态加载标记回归。

## Task 2–3 · 发布与关联更正

- 从猫主页预选、发现／已关注选猫、未知／多猫分享和发布结果回流接通；取消选择不修改草稿。媒体缺失、隐藏目标、六图顺序及正文恢复均有行为回归。
- 原帖本人菜单提供关联更正，保留原 postId、作者、评论和附件；明确选择后保存。网络未确认沿用原键，版本冲突重读并要求新选择。
- 旧猫不可见时，“我的”提供私有待更正列表与原帖修复入口；未删除／隐藏的本人原帖可更正，公开聚合规则不放宽。
- 024 前向迁移修复两种发布 RPC 的失联对账顺序：先找回原帖，再对新请求检查当前猫／媒体；原帖已删除为终态，不重建。
- 审查修复：取消强制邻里选择后清除待取消猫意图；补上 `editCat=1`；更正完成强制失效原帖正在进行的旧读取，晚到结果不覆盖新关联。
- 独立客户端审查 `c1_publishing_review` 与 SQL 审查 `c1_link_sql_review`（Terra，只读），修正后无实现阻塞项。SQL 审查中关于 FOR SHARE 和锁顺序的初步疑虑已依据实际锁冲突规则撤回，并用真实并发用例验证。
- 全库 **47 个 SQL 文件、1,795 项断言通过**；包括 045 的25项、046的37项及047的17项。047通过两个真实数据库会话验证双设备修改、删除／隐藏、更正与猫归档竞争，远端样本与辅助函数在正常及异常路径清理。
- 原 care/report 旅程测试同步运行新的焦点加载钩子，9项通过；报告身份写入与故事关联更正保持独立。

## 当前验证边界

- 完整 `pnpm verify` 已通过（配置校验、脚本契约、lint、类型、测试及构建）；精确源码 CI、后端部署和 IPA 核验全部通过。手机上的视觉、手势及两账户旅程保持待验收。
- 0.4.15（23）可安装候选已核验并交付；这不代表设备验收完成。
- C2 关注更新／阅读进度与 C3 相册／连续样本仍未实施；本轮不生成虚假活跃、照护或研究结果。

## 后端与候选来源

- 应用与 SQL 实现：`b8b37ec`（Task 1 为 `170c487`）；测试 UUID 静态扫描兼容修正最终为 `b32b7e6`。首轮 CI 在 UUID 静态检查失败，SQL 动态拼接本身可运行；现已改为完整样本字面量，并通过 `node scripts/pilot-gate-inputs.mjs uuid`、045 的25项与047的17项回归。保留失败记录，不称首轮 CI 通过。
- `pnpm verify` 最终本地通过，其中 mobile **145 套件／1,284 项**。全库47文件1,795断言；定向修改后受影响 SQL 复跑通过。
- Hosted Gate 2B **34750862354** 成功，023／024已部署；迁移头024与函数树证据已晋升，`validate:gate-2b-readiness`通过。
- 线上匿名实读 Biscuit 的故事、附件标识和原帖作者／正文一致；私有更正／修复列表RPC拒绝匿名。见 [线上读取证据](2026-09-13-c1-public-smoke.json)。两作者合成 SQL 旅程已过，两个真实手机账户尚待验收，不混写。
- 0.4.15（23）[原生构建 34751232310](https://github.com/ZP151/anicare/actions/runs/34751232310) 成功，固定源码 `e7550d29eaa2fb75efeffaad7dc510e89d4e4099`；精确源码 [CI 34751230961](https://github.com/ZP151/anicare/actions/runs/34751230961) 与 [CI 34751228284](https://github.com/ZP151/anicare/actions/runs/34751228284) 均通过，含数据库合同检查。


## 0.4.15（23）候选交付核验

- [安装包](C:/Users/15492/Downloads/WhiskerCommons-0.4.15-build23.ipa)；原始三文件目录 `C:/Users/15492/Downloads/WhiskerCommons-iPhone-34751232310`。
- Artifact `10316431044`，名称 `whiskercommons-unsigned-e7550d29eaa2fb75efeffaad7dc510e89d4e4099`；IPA **21,957,734 bytes**，SHA256 `a810316c15f4cb9e5e25ca6b7fb60116f60f3351756172a94e21a2e11f815130`。
- GitHub attestation 验证固定 workflow、main ref、精确源码与托管 runner；三文件白名单、严格 manifest、校验文件、大小、SHA256 及源码 pnpm／Pod lock 一致。
- 包内版本 **0.4.15（23）**，bundle `sg.animalhelper.app`，原生 arm64；AppIcon、非空图标资源、Assets.car 与相机／照片／使用时定位说明均通过检查。友好命名副本与原始 IPA 字节一致。
- 集中测试单已更新，保留 0.4.14 静态刷新标记回归及 C1 五组待测旅程。没有把自动检查、匿名线上读取或合成 SQL 用例写成真实用户验收。
- 后续文档提交不改变本候选源码；C2、C3 和真实研究指标仍未完成。

## 安装后反馈 · 2026-09-13

- 用户已安装 0.4.15，确认静态刷新标记及此前版本基本通过；C1 功能尚未找到入口，不能视为 C1 通过。
- 核对源码：新入口在原帖猫名胶囊→猫主页的“共同故事／分享它的故事”，以及发帖的“关联猫咪（可选）”；首页 C2 尚未改版。原验收单只写“进入猫主页”，缺少到达路径。
- 已补充单账户四步主路径、备用地图入口和可见文字，保留两账户及异常路径待测。功能可发现性仍有改进空间；本次不声称已在用户手机复现或已修复 UI。

## C1 真机拒收 · 2026-09-13

用户确认 C1 不通过：旧样本帖子无猫入口、图片大量复用且有缺图，返回／作者栏随内容滚动。新建帖子存在关联入口不代表完整旅程通过。修正计划见 [验收修正](../superpowers/plans/2026-09-13-c1-acceptance-repair.md)。静态刷新标记及此前基本通过记录保留。
