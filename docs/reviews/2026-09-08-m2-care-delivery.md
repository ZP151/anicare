# M2 照护闭环交付记录 — 2026-09-08

M2.1–M2.3 本地功能已完成，推进 G3 / F04 / C05。贡献者可记录已完成照护，另一位查看者在策略允许后读取粗略历史，本人可更正或撤回。基线仍为 `6797215` 加当前未提交工作树，依赖此前 M1 迁移；不把本地通过当作已合入或产品效果达成。

## 交付行为

`202609080005_completed_care.sql` 增加窄写入、公开历史、本人历史、更正和撤回 RPC；私有请求账本及失效关系维持默认拒绝权限，raw care_events 客户端权限未开放。重复请求返回同一事实，更正追加替代事实并让原事实立即退出公开投影，撤回保留本人可识别的失效状态。动物删除后的重试返回不可用，不能以空 ID 假报成功。

活动仅包含 feed、water、cleanup、observe、companionship。记录的是已完成事实，非预约、急救请求或派单；不公开自由文本。公开来源明确为 reported，页面说明未经独立核实。没有记录不等于无人照护。

首版手选 Jurong、MacRitchie、Tampines 三处现有粗略区域；三个格子由仓库 h3-js 验证有效、分辨率 9 且在既有 Singapore bounds 内。SQL 精确允许这三个格子；新记录区域初值为空，完成时间支持设备当地日期/时间且限定过去 30 天，不能由历史目击位置代填。本人的更正表单显示原照护的字段供修改，不引用猫历史目击地点。

新写入需成年资格及 M1 当前公开猫资格。当前 linked、非 archived 来源的最高风险参与策略，包括 hidden critical；公开读取也重验风险升级、目标隐藏/删除及双向屏蔽。普通/敏感分别延迟 2/24 小时，自服务端接收时间计算，因此回填已完成时间不能提前公开。本人撤回不依赖公开猫资格或重新声明成年。

猫详情可进入照护页；公开历史支持分页、刷新、空态及失败重试。Profile 和照护页均提供“我的照护记录”，可独立查看、更正、撤回，并遍历本人记录。账户切换立即移除旧视图，旧异步响应与发送前后账户校验共同保护状态。

待确认操作在原生 SecureStore 中按 owner 保存；跨页面/重新打开复用同一请求和 payload，写入前保存、服务端确认后清除。同一 owner 的本地写操作串行，第二条未确认操作不能覆盖第一条。响应丢失时仍可重试；显式“停止重试”只清本地待办，界面说明不会撤销服务端可能已保存的记录，并提示先查看本人历史。

## 验证与修复

- 026 先在旧 004 上确认 RED；初次迁移遗漏 visible_at 列回滚，补齐后又由实际调用发现错误风险枚举。最终函数与 schema 已按当前 005 本地应用。
- 完整 C05 SQL：51 断言通过，覆盖第二位查看者延迟前后、更正追加/失败回滚、双方屏蔽、风险升级/hidden critical、分页、隐藏后撤回、删除后重放。真实两个会话重叠同请求，均得到一个相同 careEventId；合成并发账户/动物/账本/审计记录显式清理，普通旅程事务回滚。
- 移动端真实 CatRoute→照护路由→adapter 写入、重开恢复、公开分页、本人更正、撤回重试、账户变化和本地并发存储均有行为断言。初版失效的分页/窄返回/并发覆盖先红后修。
- `pnpm test:product-core`：mobile 20 files / 238 tests（16.397 秒）与 Admin 5 files / 17 tests（2.43 秒）通过。只在快捷组合加入成熟照护行为，CI 未新增重复 job。
- `supabase test db --local`：26 files / 1,370 assertions，通过。精确 migration / SQL / remote inventory 同步到 005 / 026；输入发现测试 3 项通过。
- `supabase db lint --local --level warning` exit 0；保留既存 PostGIS 静态分析诊断、旧 AI/建档变量警告，以及 withdraw_care_event 的未使用行变量提示，未宣称零警告。
- 根 `pnpm verify` exit 0：lint/typecheck、所有工作区测试、7 个构建任务通过；mobile 78 suites / 914 tests，Admin 8 files / 56 tests。未改变的工作区使用缓存，build 为 5 cached / 7 total，保留既存 pilot-gate-2b 输出目录配置警告。Python 使用本地 3.12 环境；Windows CRLF 规则下 diff 空白检查通过。

一次合并 scoped 独立评审完成。评审发现的删除后撤回/更正 null 成功已修复并加红/绿 SQL；最终 SQL 与移动端账户/请求/公开路径无剩余实质问题。未增加全项目复审、目标参考照片要求或新发布门禁。

## 边界与下一步

未 commit/push/merge、未部署、未远端 CI 或实机验收。原生 SecureStore 重开语义由 API 边界测试验证，真实设备 Keychain/Keystore、网络断连和前后台需在对应候选上测；本批未承诺完整 Web 离线写入或后台照护队列。真实照护完成率、记录被第二位照护者使用比例及北极星猫数仍未测。

A0 首次仓库数据盘点已完成，见 [数据盘点](2026-09-08-a0-data-readiness.md)。真实许可图像/标签/划分缺口影响 AI 评测与 A1，不阻塞 M3。下一批按 M3.1 猫关注、M3.2 发现分页/筛选、M3.3 账户退出/隔离推进，复用 M1 档案和 M2 历史，以 C06/C07 追踪 G2/G4。
