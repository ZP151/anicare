# 0.4.8（16）· 地图构图、六图与资料编辑

用户确认目击上传流程跑通；图二为本轮全岛默认构图依据。已通过的同排原生玻璃导航保持原实现。

## 实现与边界

- 地图使用实测宽高、官方陆地边界和浮层留白计算 Mercator 视野，然后提交一次 region。清除 MapKit layoutMargins 与 fitToCoordinates edgePadding 的重复适配路径；等 native-ready 与真实布局均到位后执行。Tuas / Changi / Tekong 全部保留，额外约 10% 地理留白。手动远缩不设上限；旋转后重新适配，搜索/选中社区不被重置。实际原生渲染需真机核对图二。
- 编辑照片为三列换行网格，六张呈两行；每张保留缩略图、封面选择、44pt 删除区域和加载失败重试。保留已有逐张加密保存、顺序与恢复流程。
- 报告回执的已有猫改为每页最多八个头像气泡，轻按反馈与短昵称，分页仍能访问所有公开候选；签名头像不可用时显示猫图标。已有猫、新猫、跳过、 durable request 重试及独立审核保持原语义。报告编号与时间移入详情，保留清晰状态，次级返回操作同排。
- 我的页面删除 Reports 快捷项，报告历史仍在设置/发布入口可找回。昵称旁 44pt 点击范围的铅笔进入独立资料页，集中昵称、相机、相册、默认头像；设置不再重复昵称/头像。资料输入支持键盘避让，关闭/失败/账户切换保留原有隔离规则。

## 验证

先新增边界构图、六图网格与 Edit Profile 回归，确认旧实现失败；修复后跑目标测试。补充 20 条 API 分页跨八格页不遗漏、变更 viewport 只重算一次等场景。本地完整 `pnpm verify` 通过：128 个移动端套件、1114 项测试，以及所有工作区类型检查、lint、测试与构建。独立审查复核通过，无剩余阻断项；云端原生包待构建核验。

这次不以截图或单元测试替代真实 iPhone 手势验收。个人邻里编辑、独立照片分页 API、评论信息层级与后台媒体清理调度继续按 R4 / v6 的既有计划推进，不称为已完成。
