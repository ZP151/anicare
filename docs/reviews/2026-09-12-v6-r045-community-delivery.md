# 0.4.5（13）合并交付记录

## 范围

用户将近两轮未测试改动统一并入本版；本轮选择优先浏览与发布场景。源提交 `497d77594789f5a15fe6c43ab3f473dfa966af22`。

- 48 篇服务器持久化样本帖、48 条基础回复；新增 24 篇日常故事，19 份虚构人物资料。
- 31 篇带图帖子、84 个媒体附件，覆盖 1–6 图；使用已有获准的合成素材，附件数不是独立照片数，也不表示连续实拍。
- 头像进入资料，展示人物介绍、所属邻里、公开帖子及图片张数；真实 API 读取失败显示重试。公开详情和图片读取之间内容被隐藏时，不继续显示旧内容。
- 单行原生玻璃导航、地图视野、评论输入条、地图无上下箭头及左上玻璃返回，均继承前两轮改动，仍待真机验收。

## 验证

- 完整本地 `pnpm verify` 通过；最后增加的部分媒体缺失回归单独通过。
- 最终提交 CI [34684556546](https://github.com/ZP151/anicare/actions/runs/34684556546) 成功，126 套移动端测试、1092 项通过，另包含数据库契约与项目构建检查。
- 样本 provisioner 10 项测试、typecheck、只读 dry-run 通过。保留历史帖子和前 13 个附件顺序，重复执行不覆盖现存数据或复活已删除内容。
- 独立 review 的图片信息失败/可见性变化问题已修正，并覆盖空结果、部分结果、路由切换迟到响应；无剩余实现阻断项。

## 部署与安装包

安装包工作流 [34684565644](https://github.com/ZP151/anicare/actions/runs/34684565644) 成功，来源仍为上述 `497d775`：

- [0.4.5（13）IPA](C:/Users/15492/Downloads/WhiskerCommons-0.4.5-build13.ipa)，21,802,273 字节。
- SHA-256：`5c78a3a882f25d954a56f3206b15b88da9bce6f88c99ca38f05aeeb763ee6b81`。
- GitHub attestation、清单来源、实际版本 0.4.5 / 13、arm64、非空图标、定位/相机/图库声明、仓库锁文件摘要全部核验通过。
- 带版本号的简短文件名与规范下载文件逐字节一致。用于避免再次选错 0.4.2 / 0.4.3 安装包。

样本工作流 `34684627077` 在公开读取校验失败后停止；独立匿名只读检查确认 C01–C40 可读，C41–C48 尚未写入。缺少当时的细分错误，不将其断言为已确认的网络故障。导入器后续提交 `ef38b24198d897576c1c389b5616ef1968d293a0` 补充读取最多三次重试、750/1500 ms 退避以及仅包含样本编号/错误布尔值的诊断；严格图片 SHA 与正文校验保持，不重复写操作。13 项导入器测试、typecheck、dry-run 和独立 review 通过。续跑工作流 `34685499995` 已完成 C48 写入，后续图片读回耗时较长而被取消，不能记为成功工作流。随后本机通过公开接口严格核验全部 48 篇正文、48 条基础回复、反应投影、84 个有序媒体附件，以及 168 个显示图/缩略图版本的 JPEG 类型和逐字节 SHA；全部通过。实际回执为 [deployed-r045-public-verified.json](../test-samples/ios-v1/deployed-r045-public-verified.json)。未删除或重置数据，也未重复整轮导入。

收尾补充单次读取 10 秒截止时间，每次使用新的 AbortController；公开 RPC/图片 GET 传递取消信号，Storage 签名 URL 读取以 Promise.race 限定等待。最多三次重试，仅重试读取失败，不放宽正文/图片校验。14 项导入器测试与 typecheck 通过，独立 review 的签名 URL 等待缺口已补齐。导入器提交 `ef38b24` 的 CI [34685499677](https://github.com/ZP151/anicare/actions/runs/34685499677) 成功；这些脚本及记录变更不改变移动端，IPA 来源仍为 `497d775`，无需重编。

此前样本工作流 `34684566944` 因 main 不在 hosted-gate-2b 分支白名单而未执行；改用已允许的 `codex/hosted-gate-2b`，未修改保护规则。

## 尚未完成

R4 的个人所属社区编辑、独立照片分页、设置收纳继续跟踪；Apple / Google 提供商配置、真实用户公开资料、视频、管理员 / Bot 不在本轮完成范围。浏览样本无需退出重新登录，点赞、评论及发布自己的帖子使用现有真实邮箱账户。

实际设备效果按 [统一清单](../ios-next-device-test.md) 下一次验收。任何模拟内容均不计入真实用户、真实目击或活跃度。
