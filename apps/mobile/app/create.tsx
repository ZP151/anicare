import { useRouter } from 'expo-router';
import { Pressable, StyleSheet, Text, View } from 'react-native';
import { ScreenScaffold } from '../src/components/ScreenScaffold';
import { AppIcon } from '../src/components/AppIcon';
import { useNativeColors } from '../src/design/native-colors';
import { useLocale } from '../src/i18n/LocaleContext';
export default function CreateScreen(){const router=useRouter(),c=useNativeColors(),{locale}=useLocale(),cn=locale==='zh-CN';return <ScreenScaffold compact title={cn?'创建':'Create'} trailing={<Pressable accessibilityRole="button" accessibilityLabel={cn?'关闭':'Close'} onPress={()=>router.back()} style={styles.close}><AppIcon name="close" color={c.actionPrimary}/></Pressable>}><View style={styles.list}><Choice label={cn?'分享帖子':'Share a post'} icon="community" onPress={()=>router.push('/community/new' as never)}/><Choice label={cn?'报告目击':'Report a sighting'} icon="reports" onPress={()=>router.push('/report' as never)}/><Choice label={cn?'帖子草稿':'Post drafts'} icon="more" onPress={()=>router.push('/community/drafts' as never)}/></View></ScreenScaffold>}
function Choice({label,icon,onPress}:{label:string;icon:'community'|'reports'|'more';onPress():void}){const c=useNativeColors();return <Pressable accessibilityRole="button" accessibilityLabel={label} onPress={onPress} style={[styles.choice,{backgroundColor:c.surface}]}><AppIcon name={icon} color={c.actionPrimary}/><Text style={[styles.choiceText,{color:c.ink}]}>{label}</Text><AppIcon name="chevron" size={16} color={c.muted}/></Pressable>};const styles=StyleSheet.create({close:{width:44,height:44,alignItems:'center',justifyContent:'center'},list:{gap:10},choice:{minHeight:56,borderRadius:16,padding:14,flexDirection:'row',alignItems:'center',gap:12},choiceText:{flex:1,fontWeight:'600',fontSize:16}});
