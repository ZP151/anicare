import { useRouter } from 'expo-router';
import { Pressable, StyleSheet, Text, View } from 'react-native';
import { SafeAreaView } from 'react-native-safe-area-context';
import { AppIcon } from '../src/components/AppIcon';
import { useNativeColors } from '../src/design/native-colors';
import { useLocale } from '../src/i18n/LocaleContext';
export default function CreateScreen(){const router=useRouter(),c=useNativeColors(),{locale}=useLocale(),cn=locale==='zh-CN';return <SafeAreaView edges={['left','right','bottom']} style={[styles.safe,{backgroundColor:c.canvas}]}><View style={styles.header}><Text accessibilityRole="header" style={[styles.title,{color:c.ink}]}>{cn?'创建':'Create'}</Text><Pressable accessibilityRole="button" accessibilityLabel={cn?'关闭':'Close'} onPress={()=>router.back()} style={styles.close}><AppIcon name="close" color={c.actionPrimary}/></Pressable></View><View style={styles.list}><Choice label={cn?'分享帖子':'Share a post'} icon="community" onPress={()=>router.replace('/community/new' as never)}/><Choice label={cn?'报告目击':'Report a sighting'} icon="reports" onPress={()=>router.replace('/report' as never)}/><Choice label={cn?'帖子草稿':'Post drafts'} icon="more" onPress={()=>router.replace('/community/drafts' as never)}/></View></SafeAreaView>}
function Choice({label,icon,onPress}:{label:string;icon:'community'|'reports'|'more';onPress():void}){const c=useNativeColors();return <Pressable accessibilityRole="button" accessibilityLabel={label} onPress={onPress} style={[styles.choice,{backgroundColor:c.surface}]}><AppIcon name={icon} color={c.actionPrimary}/><Text style={[styles.choiceText,{color:c.ink}]}>{label}</Text><AppIcon name="chevron" size={16} color={c.muted}/></Pressable>};const styles=StyleSheet.create({safe:{paddingHorizontal:16,paddingTop:10,paddingBottom:12,gap:12},header:{minHeight:44,flexDirection:'row',alignItems:'center'},title:{flex:1,fontSize:20,lineHeight:25,fontWeight:'700'},close:{width:44,height:44,alignItems:'center',justifyContent:'center'},list:{gap:8},choice:{minHeight:48,borderRadius:14,paddingHorizontal:12,flexDirection:'row',alignItems:'center',gap:10},choiceText:{flex:1,fontWeight:'600',fontSize:16}});
