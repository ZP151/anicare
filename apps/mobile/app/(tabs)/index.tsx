import {SocialHome} from '../../src/community/SocialHome';

/** The Home tab stays public: it only shows the same approved community posts as /community. */
export default function HomeScreen() {
  return <SocialHome />;
}
