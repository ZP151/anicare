jest.mock('expo-location',()=>({getForegroundPermissionsAsync:jest.fn(),requestForegroundPermissionsAsync:jest.fn(),hasServicesEnabledAsync:jest.fn(),getCurrentPositionAsync:jest.fn(),Accuracy:{Balanced:3}}));
import * as Location from 'expo-location';
import { requestDeviceLocation } from './device-location';
beforeEach(()=>{jest.resetAllMocks();jest.mocked(Location.getForegroundPermissionsAsync).mockResolvedValue({granted:false,canAskAgain:true} as never);jest.mocked(Location.requestForegroundPermissionsAsync).mockResolvedValue({granted:true,canAskAgain:true} as never);jest.mocked(Location.hasServicesEnabledAsync).mockResolvedValue(true);jest.mocked(Location.getCurrentPositionAsync).mockResolvedValue({coords:{latitude:1.315,longitude:103.764}} as never);});
it('requests permission then returns the device fix',async()=>{expect(await requestDeviceLocation()).toEqual({kind:'granted',latitude:1.315,longitude:103.764});expect(Location.requestForegroundPermissionsAsync).toHaveBeenCalledTimes(1);});
it('does not prompt again if permission is already granted',async()=>{jest.mocked(Location.getForegroundPermissionsAsync).mockResolvedValue({granted:true,canAskAgain:true} as never);await requestDeviceLocation();expect(Location.requestForegroundPermissionsAsync).not.toHaveBeenCalled();});
it('returns a denied state when Settings is required',async()=>{jest.mocked(Location.getForegroundPermissionsAsync).mockResolvedValue({granted:false,canAskAgain:false} as never);expect(await requestDeviceLocation()).toEqual({kind:'denied',canAskAgain:false});expect(Location.requestForegroundPermissionsAsync).not.toHaveBeenCalled();expect(Location.getCurrentPositionAsync).not.toHaveBeenCalled();});
it('distinguishes disabled device services from a GPS request',async()=>{jest.mocked(Location.hasServicesEnabledAsync).mockResolvedValue(false);expect(await requestDeviceLocation()).toEqual({kind:'services_off'});expect(Location.getCurrentPositionAsync).not.toHaveBeenCalled();});
